/* day 15 at 100 days of coding challange 

problem : to calculate the average of an given array .

*/

class ar {
    public static void main(String[] args) {
       int[] a = new int[]{20, 30, 25, 35, -16};
        int sum = 0 ;
        for (int i=0;i<a.length;i++)
         {
             sum=sum+a[i];
             
         }
        double average = sum/a.length;
        System.out.println(average);
    }
}
